<%= content_for :title, "Detalhes do funcionário" %>

<div class="container mt-4">
  <% if notice %>
    <div class="alert alert-success">
      <%= notice %>
    </div>
  <% end %>

  <div class="mb-4">
    <%= render @funcionario %>
  </div>

  <div class="d-flex justify-content-between">
    <div>
      <%= link_to "Voltar para funcionários", funcionarios_path, class: "btn btn-secondary" %>
    </div>
    <div class="d-flex">
      <%= link_to "Editar funcionário", edit_funcionario_path(@funcionario), class: "btn btn-warning me-2" %>
      <%= confirm_delete_button "Excluir funcionário", @funcionario, { class: "btn btn-danger" } %>
    </div>
  </div>
</div>
